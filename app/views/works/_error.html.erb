<% if @work.errors.any? %>
<div id="error" class="alert alert-warning">
  <h5><%= pluralize(@work.errors.count, "error") %> prohibited this work from being saved:</h5>
  <ul>
    <% @work.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

